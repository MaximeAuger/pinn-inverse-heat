name: pinn-heat
channels:
  - pytorch
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch>=2.0
  - numpy
  - matplotlib
  - jupyter
  - ipykernel
  - pillow          # for GIF saving
  - pip
